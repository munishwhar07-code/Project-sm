PORT=5000
MONGO_URI=mongodb://localhost:27017/nest-mate
JWT_SECRET=nestmate_super_secret_key_2026

# ── OR use MongoDB Atlas (free cloud DB) ──────────────────────
# 1. Go to https://cloud.mongodb.com
# 2. Create a free cluster
# 3. Click Connect → Drivers → copy the connection string
# 4. Replace <username>, <password>, <dbname> below:
#
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/nestmate?retryWrites=true&w=majority