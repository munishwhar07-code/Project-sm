# When using the Vite proxy (recommended for local dev),
# use /api as the base — Vite forwards it to localhost:5000
VITE_API_URL=/api

# If you deploy the backend separately (e.g. Railway, Render),
# replace with the full URL:
# VITE_API_URL=https://your-backend.railway.app/api